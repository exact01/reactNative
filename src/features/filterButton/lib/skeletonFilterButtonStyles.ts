import { StyleSheet } from 'react-native'
import { RADIUS } from '@shared/lib/constants'

export const skeletonFilterButtonStyles = StyleSheet.create({
    loader: {
        marginRight: 8,
        borderRadius: RADIUS.r16,
    },
})
