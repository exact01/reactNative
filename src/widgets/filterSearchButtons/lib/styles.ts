import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
        marginBottom: 34,
        marginLeft: 30,
        marginTop: 19,
    },
    content: {
        flexDirection: 'row',
    },
})
